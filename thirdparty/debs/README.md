# Offline Debian Package Bundle

This directory is reserved for offline `.deb` bundles used to install ZeroMQ
and protobuf dependencies on the MGX10V device.

Downloaded packages are intentionally ignored by git. Keep only documentation
and scripts in the repository.

