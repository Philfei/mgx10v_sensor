This directory stores local Rockchip vendor runtime libraries.

The binary libraries are intentionally not tracked by git. Keep them in the
working tree when building or packaging `sensor_data`.

Expected target layout:

```text
rockchip/aarch64-linux-gnu/lib/
```
